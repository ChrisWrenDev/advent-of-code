[project]
name = "aoc-{{YEAR}}-{{DAY}}"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]

